<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.go.dkksemarang.bidikcovid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bidik_covid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bidik_covid_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.TambahPasienActivity"></activity>
        <activity android:name=".ui.DetailPasienActivity" />
        <activity
            android:name=".ui.SearchPasienActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>